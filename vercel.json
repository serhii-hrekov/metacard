{
  "version": 2,
  "builds": [
    {
      "src": "metacard/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "metacard/main.py"
    }
  ],
  "buildCommand": "pip install poetry && poetry export -f requirements.txt --without-hashes -o requirements.txt && pip install -r requirements.txt"
}
